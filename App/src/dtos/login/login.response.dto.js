class LoginResponse {
    constructor({
     accessToken: string,
     refreshToken: string
    }) {
            this.accessToken = user;
            this.refreshToken = password;
    }
}

module.exports = LoginResponse;